# 迭代优化方向 → Subagent / Skill 映射

> **目的**：向 PM 给出方向时，快速选对 **子代理、Skill 与文档路径**，避免「只有一句 /pm 不知道下一步 @ 谁」。  
> **原则**：多数方向是 **组合使用**（例如研发 + 质量）；**不**与业务领域一一对应的「第六个固定 Subagent」——**运维/流程/文档** 用下文 **归口** 到现有能力。

---

## 1. 能否覆盖？

| 你的意图（示例） | 是否已有归口 | 说明 |
|------------------|--------------|------|
| 产品研发（功能/算法/UI/API） | 是 | 主线 **A→B→C** + `/implementation` |
| 质量专项（测试、AC、缺陷、CI 绿） | 是 | `/quality-gate` + `/test-delivery` + `xs-testing` |
| 用户体验优化 | 是 | 侧重 **`/product-experience`**，可并行其他域 |
| 项目管理流程优化 | 是 | **`/pm`** + Harness/PM Skill；流程 **成文** 用 **`/doc-steward`** |
| 需求规划与梳理 | 是 | **`/pm`** + 路线图/章程；细化用 **`multi-agent-review`**（压缩） |
| 运维专项（部署、打包、环境、监控、脚本） | 是（组合） | **无单独「运维」Subagent**；归口 **`/systems-contract`** + [`docs/guides/部署说明.md`](../docs/guides/部署说明.md) + [`.cursor/rules/ci-pr-babysit.mdc`](rules/ci-pr-babysit.mdc) + 可选 **`xs-iteration-intents`** 下「运维」行 |
| 文档/规范/目录治理 | 是 | **`/doc-steward`** + `xs-doc-steward` |
| 版本与发版叙事 | 是 | `xs-release-versioning` + [`版本与发布.md`](../docs/product/版本与发布.md) |
| 防文档空转 / 算法误改 / 架构无留痕 | 是 | **`xs-skill-guardrails`**（与 PM、研发、文档 Skill 配合） |

---

## 2. 方向速查表（主路径）

| 方向 | 建议首启 Subagent | 主要 @ Skill | 典型产出 / 备注 |
|------|-------------------|--------------|-----------------|
| **产品研发** | `/pm` → `/consensus-review` 或分域 → `/implementation` | `xs-rd-implementation`、`xs-pm-harness` | 路线图、章程、代码与契约 |
| **质量专项** | `/quality-gate`（审视）→ `/test-delivery`（执行） | `xs-testing`、`xs-test-delivery`；PM 可 **一次规划多迭代**（拆分见 `xs-role-project-manager` / `xs-pm-harness`） | 测点、自动化、DoD 证据 |
| **用户体验优化** | `/product-experience` | `xs-role-product-experience` | 流程/可学性、文案与信息架构 |
| **项目管理 / Harness 流程** | `/pm` | `xs-role-project-manager`、`xs-pm-harness` | 路线图、范围冻结、Agent 切分 |
| **需求规划与梳理** | `/pm` | 同上 + `xs-multi-agent-review`（按需） | 需求进 `docs/product/`，迭代进 `docs/iterations/` |
| **流程成文与仓库规范** | `/doc-steward` | `xs-doc-steward` | `docs/CONVENTIONS`、索引、链接 |
| **运维与交付工程** | `/systems-contract` | `xs-role-systems-contract` + 部署/CI 路径见 §3 | 进程模型、打包、工作流、可重复部署 |
| **发版与版本号** | （PM 决策）需要时 `@` 文档 | `xs-release-versioning` | 版本与迭代 ID 表、`RELEASE_CHECKLIST` |

---

## 3. 运维专项：归口说明（重要）

本仓库 **不** 单独提供 `/ops` Subagent，避免与 **系统与契约** 重复。运维类迭代请：

1. **`/systems-contract`**：打包、进程、端口、Electron 与后端边界、可观测性挂钩的契约。  
2. **文档**：[`docs/guides/部署说明.md`](../docs/guides/部署说明.md)、[`docs/product/RELEASE_CHECKLIST.md`](../docs/product/RELEASE_CHECKLIST.md) 安装包相关章节。  
3. **CI/CD**：改 `.github` 时启用规则 **`ci-pr-babysit`**；与 Skill **`xs-testing`** 中的 CI 说明一致。  
4. 若任务以 **脚本/工作流** 为主、少改产品代码：仍可用 **`/implementation`** 小步提交，但审视以 **`systems-contract`** 为准。

---

## 4. 复合方向（常见）

| 组合 | 建议顺序 |
|------|----------|
| 研发 + 质量 | 范围冻结后：`/implementation` → `/test-delivery` **同迭代内**串或分会话；合并前 **C 阶段 DoD**。 |
| 体验 + 产品 | 先 `/product-experience`（A）再 `/implementation`（B）。 |
| 需求梳理 + 文档治理 | `/pm` 定范围 → `/doc-steward` → `docs/` 结构。 |
| 流程优化 + 版本发版 | `/pm` + `xs-release-versioning` + `RELEASE_CHECKLIST`。 |

---

## 5. 与五领域审视的关系

「产品研发」类默认仍走 **五领域 A 阶段**，但 **按迭代冻结范围裁剪**：无关域由 PM 标 **跳过**（见 `multi-agent-review`、`.cursor/README` § Subagents）。**质量 / 体验 / 运维** 专项可 **优先** 启用对应行（quality-gate / product-experience / systems-contract），不必五次全开。

---

## 6. 入口索引

| 资源 | 路径 |
|------|------|
| 本仓库 `.cursor` 总览 | [`.cursor/README.md`](README.md) |
| PM 斜杠命令 | [`.cursor/commands/pm-迭代启动.md`](commands/pm-迭代启动.md) |
| Agent 能力与命令 | [`.cursor/AGENTS.md`](AGENTS.md) |
| 流程闸口 Skill | [`.cursor/skills/xs-skill-guardrails/SKILL.md`](skills/xs-skill-guardrails/SKILL.md) |

*更新本表时：保持与 `README.md` 中 Skills/Subagents 表一致；新增 Subagent 时追加 §2。*
