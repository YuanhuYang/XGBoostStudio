# XGBoost Studio · 产品概览

> **版本对应**：v0.3.0  
> **最后更新**：2026-04-06

---

## 一、产品定位

XGBoost Studio 是一款 **XGBoost 垂直领域的专业建模平台**，以桌面应用形式交付，**完全本地运行，无需联网**。

核心价值主张：
- 数据分析师和算法工程师无需编写代码，即可完成 XGBoost 全生命周期建模
- 内置 XGBoost 专属分析逻辑（IV/KS/PSI/单调性/泄露检测），而非通用统计分析
- 5 阶段分层调优流程，完整还原专家级调优决策逻辑，100% 可追溯
- 一键生成 12 章专业 PDF 报告，可直接用于汇报、评审、合规审计

### 与竞品的核心差距

| 竞品 | 核心短板 | XGBoost Studio 优势 |
|------|----------|---------------------|
| JASP 0.96 | XGBoost 非原生支持，基于 R gbm 包；无调优过程追溯 | 原生 XGBoost、5 阶段完整调优记录 |
| JMP Pro 16 | 需额外插件；无 OOT 验证、无泄露检测 | 内置全链路分析，含 OOT、泄露检测 |
| H2O.ai / DataRobot | 黑盒调参，无法还原决策逻辑；私有化成本高 | 完全本地、调优全程透明可复现 |
| 代码库（XGBoost + Sklearn） | 门槛极高，90% 工作需手动完成 | 可视化全流程，零代码输出专家级结论 |

---

## 二、用户画像

### 业务分析师（非技术背景用户）
- **背景**：聚焦业务决策，无需掌握算法细节，熟悉 Excel 和业务数据
- **核心诉求**：上传数据 → 快速得到可靠模型 → 一份可以直接用于汇报的专业 PDF
- **产品响应**：**智能向导模式（6步全程引导）**，每步自动推荐配置，输出自然语言结论，一键生成专业报告

### 成长期研究者（在校生、算法自学者）
- **背景**：正在系统学习机器学习，希望通过实操加深对 XGBoost 参数机理的理解
- **核心诉求**：理解每个参数的含义，直观看到调参效果，培养建模直觉
- **产品响应**：**学习模式（向导叠加层）**，每个参数附教学卡片（算法直觉 + 调大/调小效果 + 过拟合风险条），内置"参数实验室"对比不同配置的训练曲线

### 算法专家（数据科学家、算法工程师）
- **背景**：有 XGBoost 实战经验，需要精细调参、多版本对比、快速实验迭代
- **核心诉求**：完整控制建模流程，全局状态自动传递，多模型横向对比
- **产品响应**：**专家模式（完整侧边栏）**，十页功能模块全开，全局 split_id / model_id 自动传递，模型注册表多维对比

---

## 三、三种体验模式

三种模式共享同一套后端与数据流，全局状态（activeDatasetId / activeSplitId / activeModelId）在切换时完整保留。

```
┌────────────────────────────────────────────────────────────────────────┐
│  模式切换器（Header 右侧）                                               │
│  🎯 向导模式   📚 学习模式   ⚙️ 专家模式                                 │
└────────────────────────────────────────────────────────────────────────┘
         │                   │                   │
    6步引导流程           向导 + 教学卡片      完整十页侧边栏
    自动推荐配置          参数实验对比         全局状态传递
    自然语言结论          学习面板展开         多模型对比注册表
    一键生成报告          数据驱动解释         专家级调优轨迹
```

### 智能向导 6 步流程

```
Step 0: 数据准备       → 上传/选择数据集，查看质量评分（0-100），推断任务类型
Step 1: 特征分析       → 自动完成 IV/KS/PSI/单调性分析，标记关键特征
Step 2: 特征工程       → 一键缺失值填充、编码、划分（含时间序列划分）
Step 3: 参数推荐       → 基于数据特性自动推荐 XGBoost 参数
Step 4: 模型训练+评估  → 训练 + K折交叉验证 + 过拟合诊断
Step 5: 报告生成       → 一键输出 12 章专业 PDF 报告
```

---

## 四、功能模块总览

| 模块序号 | 模块名称 | 核心功能 |
|----------|----------|----------|
| 0 | 智能向导 | 三模式入口、6步向导流程 |
| 1 | 数据导入与预览 | CSV/XLSX 导入、质量评分、缺失值/异常值/重复行处理 |
| 2 | 特征分析 | IV/KS/PSI/单调性分析、泄露检测、标签专项分析、VIF、PCA |
| 3 | 特征工程与选择 | 编码/缩放/Box-Cox/选择、时间序列与随机划分 |
| 4 | 参数配置 | 参数推荐、自定义配置、参数说明教学卡片 |
| 5 | 模型训练 | 实时 SSE 进度、K折交叉验证、早停机制 |
| 6 | 模型评估 | 混淆矩阵/ROC/PR/校准、PDP/ICE、OOT验证、鲁棒性测试、坏样本诊断 |
| 7 | 超参数调优 | 5阶段分层调优、调优轨迹图、各阶段参数锁定与传递 |
| 8 | 模型管理 | 模型注册表、多维对比、McNemar 检验、版本管理 |
| 9 | 分析报告 | 12章 PDF 报告、4种预设模板、品牌定制 |
| 10 | 交互式预测 | 文件批量预测 + 表单手动预测、SHAP 单样本解释 |

---

## 五、产品版本历史

| 版本 | 时间 | 核心变更 |
|------|------|----------|
| v0.1.0 | 早期 | 基础 XGBoost 训练/评估/报告框架搭建 |
| v0.2.0 | 中期 | 模型管理、调优、K折、运行档案（G1/G2 迭代） |
| v0.3.0 | 2026-04 | G3三域重构：IV/KS/PSI/泄露检测、5阶段调优、12章报告、PDP/ICE、鲁棒性测试 |
