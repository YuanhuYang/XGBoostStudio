# 验收执行记录 · G2-Auth-1 可复现与运行档案（2026-04-05）

> **章程**：[`迭代章程-G2-Auth-1-可复现与运行档案.md`](迭代章程-G2-Auth-1-可复现与运行档案.md)  
> **矩阵**：[`验收-AC证据矩阵.md`](验收-AC证据矩阵.md) **G2-Auth-1 运行档案** 小节  
> **缺口**：[`迭代计划.md`](迭代计划.md) **GAP-G2-AUTH-001** → **已完成**  
> **与全量关系**：本记录为 **G2+ 专项**；[`功能需求与验收状态.md`](功能需求与验收状态.md) §二 **全表通过** 仍须单独执行；汇总索引见 [`验收执行记录-全量验收对齐-20260405.md`](验收执行记录-全量验收对齐-20260405.md)。

## 1. 自动化门禁

| 命令 | 目录 | 结果 |
|------|------|------|
| `uv run pytest -q` | `server/` | **通过**（含 `test_provenance_g2_auth1.py` 全文件；以仓库当前 CI 通过数为准） |

| 用例 | 章程 / 矩阵对应 |
|------|-------------------|
| `test_provenance_after_training_matches_model_record` | §2.1：档案字段与 `params`/`metrics` 同源 |
| `test_provenance_404_unknown_model` | §2.1：未知模型 404 |

## 2. 脚本式验收（需 HTTP 服务）

| 命令 | 前提 | 说明 |
|------|------|------|
| `uv run python tests/acceptance_test.py` | `server/`、`127.0.0.1:18899` 已启动 API | 步骤 **[7/8]～[8/8]** 覆盖训练 SSE + `GET /api/models/{id}/provenance` |

CI：`.github/workflows/acceptance.yml` **workflow_dispatch**（不阻塞 PR）。

## 3. DoD 核对（测试顾问）

- [x] 章程 §2.1 P0：`provenance_json` + `GET /api/models/{id}/provenance` + pytest  
- [x] 矩阵 G2-Auth-1 行：自动化与 `acceptance_test` 索引已填  
- [ ] [`功能需求与验收状态.md`](功能需求与验收状态.md) §二 / §四：**本记录为 G2-Auth-1 专项**；全产品「当期发布口径」通过须另按项目经理冻结范围勾选  
- [ ] 章程 §2.2 可选项（调优档案深化、PDF「运行环境」、CI `GIT_COMMIT`）：**未纳入本记录 DoD**，按需另开迭代

## 4. 实现索引（研发）

| 项 | 位置 |
|----|------|
| 运行档案构建 | [`server/services/provenance.py`](../server/services/provenance.py) |
| 训练写入 | [`server/services/training_service.py`](../server/services/training_service.py) |
| 调优写入 | [`server/services/tuning_service.py`](../server/services/tuning_service.py) |
| ORM / 迁移 | [`server/db/models.py`](../server/db/models.py) `Model.provenance_json`；[`server/db/database.py`](../server/db/database.py) `_run_migrations` |
| API | `GET /api/models/{model_id}/provenance` — [`server/routers/models.py`](../server/routers/models.py) |

## 5. 六角色签核（仅当本章程范围全通过后）

> 与 [`功能需求与验收状态.md`](功能需求与验收状态.md) §四 格式一致；G2-Auth-1 专项可在备注引用本记录。

| 角色 | 签核 | 日期 | 备注 |
|------|------|------|------|
| 数据分析 | | | 划分种子与档案 `split_random_seed` 一致 |
| 模型训练 | | | 随机性说明见 `provenance.py` 模块注释 |
| AI 学生 | | | 帮助文案可选后续迭代 |
| 产品设计 | | | 模型页「复制档案」可选后续迭代 |
| 架构 | | | API 与 `schema_version` 演进 |
| 测试顾问（QA） | | | 矩阵 + pytest + 本记录 |

---

*本记录随矩阵与 pytest 变更更新「§1 通过数」与签核状态。*
