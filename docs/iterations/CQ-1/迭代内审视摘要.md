# CQ-1 迭代内审视摘要（裁剪）

> 按路线图仅启动 **质量门禁** + **系统与契约**；数据/ 建模 / 体验 **无失败清单命中项**，本轮跳过。

## 质量门禁（≤10 条）

1. **CI 等价性**：`pytest -q` 与 `client` test + typecheck 已与本地跑通一致，可作为合并门禁参照。
2. **全量 pytest**：81 项与 CI 收集范围一致；无跳过失败需解释。
3. **acceptance 证据**：`acceptance_test.py` 须在 **独占 18899** 的当前代码服务上跑；旧进程会导致 404，属 **操作风险** 非产品回归。
4. **Windows 编码**：验收脚本避免非 GBK 可打印字符，避免假失败（已修 ASCII 成功行）。
5. **warnings**：Pydantic `Config`、SQLAlchemy `utcnow`、tuning `utcnow` 等仅告警；不阻塞本迭代，可进技术债池。
6. **RELEASE §2**：`check_wizard` 与 `acceptance` 已执行；未跑不得勾选清单（见 [`RELEASE_CHECKLIST.md`](../../product/RELEASE_CHECKLIST.md)）。
7. **对外话术**：PR/CI 中 **不等同于** [`功能需求与验收状态.md`](../../product/功能需求与验收状态.md) 全量 AC；CQ-1 记录已对齐。

## 系统与契约（≤10 条）

1. **端口与进程**：本地验收前释放 `127.0.0.1:18899`，避免命中陈旧后端实例。
2. **Python 真相来源**：脚本式检查与验收用 **`uv run`**，与 `pyproject`/lockfile 一致。
3. **B 档前置**：Electron 打包依赖 `scripts/build-server.ps1` 与 `build-client.ps1` 与 NSIS 链；将在 **CQ-2** 实测并记录 [`RELEASE_CHECKLIST.md`](../../product/RELEASE_CHECKLIST.md) §6。
4. **契约一致性**：acceptance 覆盖上传、wizard、训练、SSE、`provenance`；与当前路由一致。
5. **版本**：本期不发版；安装包若产出仅为 **构建验证**，不占 `版本与发布.md` 发布行除非 PM 另定。

*汇总格式对齐 [`xs-multi-agent-review`](../../../.cursor/skills/xs-multi-agent-review/SKILL.md) 的迭代内压缩要求。*
