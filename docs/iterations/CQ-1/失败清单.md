# CQ-1 失败清单

> **状态**：基线跑测完成（2026-04-05）。分级：**P0** = 阻塞 CI/商用证据；**P1** = 不阻塞 CI 但应在 CQ-2 处理。

## 结论

**无未关闭的 P0 阻塞项**（CI 等价命令与全量 pytest、前端 test/typecheck、wizard 检查、acceptance 在正确环境下均通过）。

## 已处理 / 登记项

| ID | 分级 | 现象 | 处理 |
|----|------|------|------|
| ENV-01 | — | `python tests/check_wizard.py` 使用系统 Python 时 SciPy DLL 错误 | 文档化：须 **`uv run python`**（见 [`执行记录.md`](执行记录.md) §3） |
| ACC-01 | P1→已修 | `acceptance_test.py` 末尾 `print` 含 Unicode，Windows GBK 下 `UnicodeEncodeError` | 已改为 ASCII 成功消息；**非 API 缺陷** |

## 模板（后续若回归失败再填）

| ID | 分级 | 现象 | 复现命令 / 步骤 | 日志摘要 | 建议 Owner | 状态 |
|----|------|------|-----------------|----------|------------|------|
| — | — | | | | | |
