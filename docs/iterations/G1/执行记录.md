# 验收执行记录 · G1 信任链与权威数据（2026-04-05）

> **章程**：[`章程.md`](章程.md)  
> **矩阵**：[`验收-AC证据矩阵.md`](验收-AC证据矩阵.md) **G1 信任链冻结 AC** 小节

## 1. 自动化门禁

| 命令 | 目录 | 结果 |
|------|------|------|
| `uv run pytest -q` | `server/` | **33 passed**（含 `test_trust_chain_contract.py`、`test_authority_breast_cancer_pipeline.py`） |
| `npm run test -- --run && npm run typecheck` | `client/` | 与 CI 一致 |

## 2. DoD 核对（质量门禁 / QA）

- [x] 冻结 AC 表每一行：矩阵中 **pytest** 或 **豁免** 已填写  
- [x] 豁免 **EXEMPT-G1-401**：参数表单展示 — 手工证据模板见下节  
- [x] 豁免 **EXEMPT-G1-701**：Optuna 本期不验 — 已登记  
- [ ] [`功能需求与验收状态.md`](功能需求与验收状态.md) §二 / §四：**本记录不单独触发「全表通过」**；须项目经理宣布章程范围全达成后再改状态

## 3. 手工证据模板（豁免项）

| 豁免 ID | 建议证据 |
|---------|----------|
| EXEMPT-G1-401 | 参数页截图或录屏 + 与 `GET /api/params/schema` 字段数量对照说明 |
| EXEMPT-G1-701 | 书面说明「本期不验」+ 下迭代 AC 编号（可选） |

## 4. 五领域签核（仅当章程范围全通过后）

> 与 [`功能需求与验收状态.md`](功能需求与验收状态.md) §四 一致；信任链专项可在备注引用本记录。术语见 [`项目管理术语-五领域.md`](项目管理术语-五领域.md)。

（略 — 由项目经理组织填写）

---

*本记录随矩阵与 pytest 变更更新「§1 通过数」。*
