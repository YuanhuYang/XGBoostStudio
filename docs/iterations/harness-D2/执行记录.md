# 验收执行记录（抽样 · 2026-04-05）

对应 Harness **D2**：在 [`验收标准文档.md`](../../product/验收标准文档.md) 修订版基础上，对 **模块 0 / 报告 / 预测 / 导入** 各至少抽样一条，区分 **可自动化** 与 **需 UI（A/B 档环境）**。

## 执行摘要

| 维度 | 结论 |
|------|------|
| 文档与产品对齐 | **已通过**：AC-0-01 已改为 PDF；验收环境区分 A/B 档（见验收标准文首）。 |
| API 自动化 | **已通过**：`uv run pytest -q` → **11 passed**（含 Titanic **AC-0-01 API 等效** 全链至 PDF，见下文 F1/F2）。 |
| 客户端类型 | **已通过**：`npm run typecheck`。 |
| 全链路 UI（SmartWorkflow Step 0–5） | **未在本记录中执行**：需在 **开发态 A** 或 **安装包 B** 下人工按 AC-0-01 走通并勾选验收标准中的 checkbox。 |
| 报告 PDF e2e（`e2e_validate.py` 独立进程） | **已补跑**（见文末「续」节）：临时起 `uvicorn` + 本地库有模型，`e2e_validate.py` exit 0；与 `pytest` 隔离库用例 **互补**。 |

## 抽样明细表

| 模块 | 抽样 AC / 测点 | 执行方式 | 结果 | 证据 / 备注 |
|------|----------------|----------|------|-------------|
| 0 智能向导 | AC-0-01 文档条款（PDF、步骤 5） | 文档修订 + 评审 | 通过 | [`验收标准文档.md`](../../product/验收标准文档.md) |
| 报告 | 不存在模型时生成报告 | `pytest` `test_report_generate_unknown_model` | 通过 | 404 + detail |
| 报告 | 不存在报告 id 下载 | `pytest` `test_report_download_unknown_report` | 通过 | 404 |
| 数据导入 | 非表格扩展名上传 | `pytest` `test_upload_rejects_non_tabular` | 通过 | 400 |
| 数据导入 / 向导 | iris 上传与摘要 | `test_upload_iris_*`, `test_wizard_dataset_summary` | 通过 | fixtures |
| 训练 | 创建任务（不跑满 SSE） | `test_split_and_training_start` | 通过 | `task_id` |
| 预测 | 批量预测结果摘要（任务不存在） | `pytest` `test_prediction_batch_summary_unknown_task` | 通过 | 404 |
| 0+5+报告 | AC-0-01 API 等效（Titanic→PDF） | `pytest` `test_titanic_ac001_api_pipeline_report_pdf` | 通过 | 约 10s，见 F1 节 |

## P0 缺陷

- 无（本抽样范围内）。

## P1 / 延后

- 全量 AC 手册（1200+ 行）分模块排期执行；性能类（DS-05）建议单独慢测 job，避免进入默认 PR pytest。

## M2 打包（同日执行摘录）

| 脚本 | 结果 |
|------|------|
| `scripts/build-server.ps1` | 成功，`server/dist/xgboost-server.exe` |
| `scripts/build-client.ps1` | 成功；已修复 `package.json` 无效 `onlyFn`、`win.signAndEditExecutable: false` 以规避 rcedit 失败；`build-client` 自动复制后端 exe 至 `client/resources/` |

详见 [`RELEASE_CHECKLIST.md`](RELEASE_CHECKLIST.md) 第 6 节。

---

## F1 · AC-0-01 证据（路线图「后续」批次）

| 类型 | 说明 |
|------|------|
| **A 档 · UI 点选** | SmartWorkflow Step 0–5 仍建议人工在 Electron/Web 上执行，并在 [`验收标准文档.md`](验收标准文档.md) 对应 checkbox 勾选。 |
| **A 档 · API 等效（已执行）** | `tests/test_api_core.py::test_titanic_ac001_api_pipeline_report_pdf`：`titanic_train.csv` → `PATCH` 目标列 `Survived` → 分层划分 → 训练 SSE 读尽 → `GET .../result` 为 `completed` → `POST /api/reports/generate` → `GET .../download` 为 `application/pdf` 且魔数为 `%PDF`。 |

## F2 · `e2e_validate.py` 与 pytest 关系

| 方式 | 状态 |
|------|------|
| `python tests/e2e_validate.py`（需 `127.0.0.1:18899` 已启动且 DB 有模型） | 本记录批次未跑（端口未监听）。 |
| 与脚本同路径的 **PDF 生成+下载** | 已由 **F1 表** 中 pytest 用例覆盖，可作为 CI/PR 证据。 |

## F3 · 分模块抽样表

见专文 [`抽样-F3-分模块.md`](../../evidence/抽样-F3-分模块.md)。

---

## 续 · 双轨 `e2e_validate.py`（持久服务 + 本地库）

| 步骤 | 结果 |
|------|------|
| `uv run pytest -q`（`server/`） | **11 passed**，约 8.4s |
| `npm run typecheck`（`client/`） | **通过** |
| 起 `uvicorn main:app --host 127.0.0.1 --port 18899` 后 `uv run python tests/e2e_validate.py` | **通过**（exit 0）：`GET /api/models` → `POST /api/reports/generate` → `GET .../download` 为 `application/pdf`，落盘 `tests/e2e_test_report.pdf`（约 117 KB） |

**脚本修复**：`tests/e2e_validate.py` 在 Windows 默认 GBK 控制台下对 `stdout` 做 `reconfigure(utf-8)`，避免末尾 `print("✅ …")` 触发 `UnicodeEncodeError` 导致误报失败（此前步骤已全部成功仍 exit 1）。

**说明**：本双轨与 pytest 内隔离库用例互补；**不**替代 AC-0-01 等 **A 档 UI 逐项勾选**。§二「全量验收状态」仍为 **未**，直至手册 AC 与签核收口。
