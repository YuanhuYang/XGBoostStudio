# 执行记录 · CQ-2

> **类型**：清单内修复、全量回归、B 档（安装包）工程证据。  
> **版本线**：仅主干，**不对外发版**；本节为 **构建验证**，不自动写入 [`版本与发布.md`](../../product/版本与发布.md) 发布表（除非 PM 另定）。

## 1. CQ-1 清单闭环

| 项 | 状态 |
|----|------|
| [`CQ-1/失败清单.md`](../CQ-1/失败清单.md) | 无未关 P0；ACC-01（acceptance 控制台编码）已在 [`server/tests/acceptance_test.py`](../../../server/tests/acceptance_test.py) 修复 |

## 2. 回归命令（与 CI / RELEASE §1～2 对齐）

| 命令 | 工作目录 | 结果（2026-04-05） |
|------|----------|-------------------|
| `uv sync --all-groups --frozen` + `uv run pytest -q` | `server/` | **41 passed** |
| `npm run test -- --run` + `npm run typecheck` | `client/` | **通过** |
| `uv run python tests/check_wizard.py` | `server/` | 退出码 0 |
| `uv run python tests/acceptance_test.py` | `server/` | 退出码 0（测试前已释放 **18899** 并启动当前代码 `main.py`） |

## 3. RELEASE_CHECKLIST 核对摘要

### §0 商用发布质量保障（工程口径）

| 检查项 | 结论 |
|--------|------|
| CI 绿（pytest + typecheck 与主干一致） | **满足**（本地复跑与 [`ci.yml`](../../../.github/workflows/ci.yml) 一致） |
| 当期范围对齐 / 全量 AC | **未**宣称 [`功能需求与验收状态.md`](../../product/功能需求与验收状态.md) §二 全通过 — 本期仅为 **CQ 质量 + B 档构建** |
| 签核 §四 | **未填**（非全量验收发布窗口） |
| B 档安装包 | **已**完成构建与产物存在（见 §4） |
| 对外表述 | 不宣称「迭代计划范围已验收通过」 |

### §1 PR 级门禁

| 步骤 | 结果 |
|------|------|
| `uv sync` + `uv run pytest -q` | 通过 |
| `npm ci`（或已有 node_modules）+ `typecheck` | 通过 |

### §2 产品级全量

| 步骤 | 结果 |
|------|------|
| `uv run pytest`（tests 收集范围） | 41 passed |
| `acceptance_test.py` | 通过（ASCII 成功行，Windows 可跑） |
| `check_wizard.py` | 通过 |

## 4. §6 打包验证（B 档）

| 步骤 | 环境 | 结果摘要 |
|------|------|----------|
| `.\scripts\build-server.ps1` | Windows 10 | **成功** → `server\dist\xgboost-server.exe`（PyInstaller 有常规 WARNING，见 `server\build\build\warn-build.txt`） |
| `.\scripts\build-client.ps1` | 同上 | **成功** → `client\resources\xgboost-server.exe` 已同步；`dist\XGBoost Studio Setup 0.1.0.exe`、`dist\win-unpacked\` |

**快验（RELEASE 建议）**：已验证 **构建产物生成**；**断网 / 双击安装** 主路径需人工在目标机执行（本节未代跑 UI）。

## 5. DoD

- [x] CQ-1 清单关闭或已登记  
- [x] 回归命令证据  
- [x] RELEASE §0～§2 与 §6 构建侧证据  
- [ ] 全产品 AC 与 §四签核 — **非本期范围**
