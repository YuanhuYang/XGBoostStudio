# 迭代章程 · CQ-2：修复、回归与 B 档交付证据

**迭代 ID**：**CQ-2**（质量 · 全量测试 · 商用化路线之 **第二束**）。  
**依赖**：[`CQ-1`](../CQ-1/) 冻结清单与执行记录。

**已确认**：产品版本线 **仅主干、不发版**；B 档 = RELEASE §0～§2 证据 + **§6 安装包**构建与快验摘要。

---

## 1. 范围冻结

| 项 | 说明 |
|----|------|
| 缺陷 | 仅 **CQ-1** [`失败清单.md`](../CQ-1/失败清单.md) 所列（含已修的 acceptance 控制台编码问题） |
| 回归 | CI 等价：`pytest -q`、`npm run test -- --run`、`typecheck`；补充 `uv run pytest`、`uv run python tests/check_wizard.py` |
| B 档 | [`RELEASE_CHECKLIST.md`](../../product/RELEASE_CHECKLIST.md) §6：`scripts\build-server.ps1` → `scripts\build-client.ps1`，记录产物路径与快验说明 |

## 2. 非目标

- 新功能、清单外重构。
- 对外 SemVer 发布（见 CQ-1 与 [`版本与发布.md`](../../product/版本与发布.md)）。

## 3. DoD

- CQ-1 清单项 **已关闭或已登记**；回归命令全绿。
- [`执行记录.md`](执行记录.md) 含 RELEASE §0～§2 与 §6 核对摘要（可勾选表形式）。
- 未宣称 [`功能需求与验收状态.md`](../../product/功能需求与验收状态.md) 全量 AC 通过（除非另行签核）。
