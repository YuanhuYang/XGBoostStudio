# I4-1 覆盖率缺口分析报告

> 迭代目标：质量基线，产出覆盖率缺口分级清单
> 执行日期：2026-04-05
> 环境：Windows 10, Python 3.12, Node 20, pytest 8.0.0, vitest 3.2.4

---

## 1. 基线测试结果

### 后端 Python

```
TOTAL     4671   1595    66%
42 passed in 73.43s
```

**结果**：所有 42 个测试 ✅ 通过

### 前端 TypeScript

```
All files  0% Stmts, 4.34% Branch, 4.34% Funcs,  0% Lines
1 passed
```

**结果**：1 个测试 ✅ 通过，但整体覆盖率极低（仅 `reportSections.ts` 100% 覆盖）

---

## 2. 后端覆盖率缺口分析（按模块分级）

### P0（必须补充，覆盖率 < 50%）

| 模块 | 语句 | 缺失 | 当前覆盖率 | 缺失范围描述 |
|------|------|------|------------|--------------|
| `services/feature_service.py` | 305 | 282 | **8%** | 几乎全量未覆盖：特征分析、相关性计算、缺失值统计所有核心逻辑 |
| `services/dataset_service.py` | 287 | 189 | **34%** | 文件保存、预览、分区、删除大部分逻辑未覆盖 |
| `services/wizard_service.py` | 298 | 205 | **31%** | 智能向导流程大部分未覆盖 |
| `services/prediction_service.py` | 87 | 67 | **23%** | 预测功能大部分未覆盖 |
| `services/eval_service.py` | 273 | 149 | **45%** | Bootstrap 置信区间、过拟合诊断大量未覆盖 |
| `routers/reports.py` (I3 新增模板 API) | 86 | 43 | **50%** | I3 新增模板接口刚好半覆盖，需要补充另一半 |
| `routers/datasets.py` | 157 | 59 | **62%** | 许多 CRUD 端点未覆盖 |
| `routers/models.py` | 117 | 50 | **57%** | 模型列表/删除/获取端点未完全覆盖 |

### P1（建议补充，覆盖率 50%-80%）

| 模块 | 当前覆盖率 | 缺失说明 |
|------|------------|----------|
| `services/report_service.py` | 66% | I3 新增章节选择、模板、APA 样式相关逻辑未覆盖 |
| `services/dataset_narrative_service.py` | 73% | 部分 narrative 生成逻辑未覆盖 |
| `services/chart_service.py` | 75% | 部分图表生成逻辑未覆盖 |
| `services/training_service.py` | 70% | 早停、任务取消部分逻辑未覆盖 |
| `services/tuning_service.py` | 72% | 断点续调部分逻辑未覆盖 |
| `routers/prediction.py` | 75% | 预测端点未完全覆盖 |

### P2（可选补充，覆盖率 ≥ 80%）

| 模块 | 当前覆盖率 | 缺失说明 |
|------|------------|----------|
| `db/database.py` | 88% | 仅 Windows 路径初始化几行 |
| `main.py` | 84% | 生命周期钩子几行 |
| `services/params_service.py` | 69% | 参数默认值生成部分逻辑 |

### 已全覆盖（不需要补充）

- `db/models.py` 100%
- `schemas/*` 全部 100%（Pydantic models）
- `tests/*` 全部 100%（测试本身不需要计算覆盖率）
- `services/report_methodology.py` 100%

---

## 3. 前端覆盖率缺口分析（按模块分级）

当前整体覆盖率：**~0%**（除 `src/constants/reportSections.ts` 100%）

### P0（必须补充 - 核心工具/常量）

| 模块 | 说明 |
|------|------|
| `src/utils/*` | 所有工具函数（`apiError.ts` 已有结构但无测试） |
| `src/constants/*` | 除已测文件外其他常量 |
| `src/types/*` | 类型定义不需要测试，但确保导出正确 |

### P0（必须补充 - API 客户端模块）

| 模块 | 说明 |
|------|------|
| `src/api/*` | 所有 API client 函数，需要单元测试（mock axios） |
| `src/api/reports.ts` | I3 新增模板 API 需要专项测试 |

### P0（必须补充 - Store）

| 模块 | 说明 |
|------|------|
| `src/store/appStore.ts` | 全局状态管理，核心动作需要测试 |

### P1（建议补充 - 页面组件工具逻辑）

| 模块 | 说明 |
|------|------|
| `src/pages/Report/index.tsx` | 章节选择逻辑可抽取纯函数测试 |
| 各页面 `constants.ts` | 抽取的常量需要测试 |

### 说明

React 组件测试遵循 **测试逻辑而非渲染** 原则：
- 可测试：纯工具函数、常量、类型校验、状态更新逻辑
- E2E 测试覆盖组件渲染交互，单元测试不追求 100% JSX 覆盖

---

## 4. 需要补充的测试点汇总（来自五领域审视）

### 数据与可复现（P0）

1. [P0] 超大 CSV 文件处理测试（内存/流式）→ `test_api_robustness.py`
2. [P0] 幂等性测试：同一文件重复上传验证
3. [P0] 随机种子可复现性：相同种子划分结果一致

### 建模与评估（P0）

1. [P0] 早停逻辑在分类/回归中正确触发验证
2. [P0] 相同随机种子训练结果一致验证（可复现性）
3. [P0] 训练/调优中止后资源正确释放验证
4. [P0] 分类/回归任务指标正确展示验证

### 产品体验（P0 - 需要自动化）

1. [P0] 章节全选/清空/反选后生成，验证 PDF 只含勾选章节
2. [P0] 保存模板 → 加载模板 → 生成验证
3. [P0] 默认格式 vs APA 格式生成对比验证

### 系统与契约（P0）

1. [P0] 添加 `pytest-xdist` 依赖支持并发执行
2. [P0] I3 新增报表模板 7 个 API 端点补充契约测试
3. [P0] 在 `conftest.py` 标记不可并发的测试避免冲突

---

## 5. 99.9% 覆盖率目标缺口计算

### 后端

- 总语句：4671
- 已覆盖：3076 (66%)
- 需要额外覆盖：**1567 语句**（当前缺 1595，去掉 `main.js` 等配置文件）
- 主要缺口：`feature_service`, `dataset_service`, `wizard_service`

### 前端

- 总文件约 50+ 源文件，仅 1 个有测试
- 需要补充：所有工具函数、API 客户端、常量、store 的单元测试
- React 组件本身不要求全量覆盖，但逻辑抽取需要测试

---

## 6. 结论与下一阶段建议

| 检查项 | 状态 |
|--------|------|
| 现有测试全部通过 | ✅ |
| 前端类型检查通过 | ✅ |
| P0 缺陷数 | 0（不包含覆盖率缺口） |
| 覆盖率缺口分级完成 | ✅ |

**下一阶段（I4-2）**：开始按 P0 顺序补充后端测试，包括：

1. 添加 `pytest-xdist` 依赖支持并发
2. 补充 `feature_service.py` 全量单元测试
3. 补充 `dataset_service.py` P0 缺口测试
4. 补充报表模板 API 契约测试
5. 补充数据领域审视中 P0 测试点
6. 每轮测试后重新计算覆盖率，逐步逼近 99.9%

---

*报告版本：v1.0 · 2026-04-05*
