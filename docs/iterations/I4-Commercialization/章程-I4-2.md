# 迭代章程 · I4-2：缺陷修复与商用化收口

> **项目经理**：在 I4-1 质量基线基础上，修复 P1 分级缺陷，版本对齐到 0.3.0，完成发版前准备。
> **依赖**：I4-1 质量基线（全量验收完成，缺陷清单已产出）。
> **迭代 ID**：I4-2（属于 I4 商用化交付）。

---

## 1. 目标

| 现状 | 目标 |
|------|------|
| 存在 P1 弃用警告（Pydantic v2 config / datetime.utcnow） | 升级到最新 API 消除警告 |
| PyInstaller 构建有字体搜索警告 | 修复配置消除警告 |
| 版本号仍为 0.2.0 | 对齐到 0.3.0，更新发布记录表 |
| 未执行发版检查清单 | 完成 RELEASE_CHECKLIST |

**范围冻结（本期必须做）**：
- 修复 P1-01：Pydantic v2 弃用警告 → 将所有 `class Config:` 替换为 `model_config = ConfigDict()`
- 修复 P1-02：`datetime.utcnow()` 弃用警告 → 替换为 `datetime.now(datetime.UTC)`
- 修复 P1-03：PyInstaller 构建字体搜索警告 → 检查 spec 文件或数据文件配置
- 版本对齐：更新 `client/package.json` 和 `server/pyproject.toml` 到 `0.3.0`
- 更新 [`docs/archive/legacy-product/版本与发布.md`](../../archive/legacy-product/版本与发布.md) 发布记录表，添加 `0.3.0` 条目
- 走完 [`docs/archive/legacy-product/RELEASE_CHECKLIST.md`](../../archive/legacy-product/RELEASE_CHECKLIST.md) 发版检查清单，打勾完成项

**非目标（本迭代不做）**：
- P2 缺陷（Bootstrap 未完整验证 / 前端覆盖率低等）→ 延后，不阻塞 0.3.0
- 新增功能 → 不做
- 大范围重构 → 不做
- 文档增强 → I4-3 处理

---

## 2. 设计约束

1. **兼容**：保持 API 接口不变，仅语法升级消除警告
2. **不改变行为**：修复后功能语义完全一致
3. **版本同步**：前端后端版本号必须严格一致

---

## 3. 优先级清单

| 编号 | 任务 | 优先级 | 说明 |
|------|------|--------|------|
| 1 | Pydantic ConfigDict 迁移 | P0 | 必须完成消除警告 |
| 2 | datetime.utcnow() 替换 | P0 | 必须完成消除警告 |
| 3 | PyInstaller 字体警告修复 | P1 | 构建体验优化 |
| 4 | 版本号对齐 0.3.0 | P0 | 发版前置要求 |
| 5 | 更新发布记录表 | P0 | 发版前置要求 |
| 6 | 完成 RELEASE_CHECKLIST | P0 | 发版前置要求 |

---

## 4. 完成定义（DoD）

- [ ] 所有 Pydantic schema 文件已迁移到 `ConfigDict` 语法
- [ ] 所有 `datetime.utcnow()` 已替换为 `datetime.now(datetime.UTC)`
- [ ] 后端 `pytest` 全量通过，无新增失败
- [ ] 前端 `npm run typecheck` 通过，无类型错误
- [ ] 前端后端版本号均为 `0.3.0`
- [ ] 发布记录表已追加 0.3.0 条目
- [ ] RELEASE_CHECKLIST 所有项已检查打勾
- [ ] 构建验证：PyInstaller 构建无 WARNING 级字体警告

---

## 5. 测试策略

- **必须跑**：
  ```bash
  # 后端
  cd server && uv run pytest -q

  # 前端
  cd client && npm run typecheck
  cd client && npm run test -- --run
  ```

---

## 6. 建议上下文边界

- **独立会话**：本迭代建议新开独立 Agent 会话执行
- **审视阶段**：无需五领域重新审视，范围来自 I4-1 缺陷清单
- **执行阶段**：`/implementation` 执行修复，`/test-delivery` 验收
- **工作量估算**：1-2 人日可完成

---

## 7. 与下一迭代 I4-3 的交接物

| 交接物 | 说明 |
|--------|------|
| P1 缺陷全部修复 | 所有 P1 项目已完成，测试通过 |
| 版本号已对齐 | 前端后端均为 0.3.0 |
| 发版检查完成 | RELEASE_CHECKLIST 已走完 |

---

*章程版本：v1.0 · 2026-04-05*
