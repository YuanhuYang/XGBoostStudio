# 验收执行记录 · 全量验收对齐与测试覆盖（2026-04-05）

> **类型**：测试覆盖迭代落地 + 项目经理发布口径冻结（见 [`功能需求与验收状态.md`](功能需求与验收状态.md) §二脚注）。  
> **说明**：本记录 **不** 表示「全量验收已通过」——[`功能需求与验收状态.md`](功能需求与验收状态.md) §二各轮次仍为 **未**，直至当期口径内全部 AC 有证据且 §四签核完成。

## 1. 自动化门禁（研发/CI）

| 命令 | 工作目录 | 结果（本记录写入时） |
|------|----------|----------------------|
| `uv run pytest -q` | `server/` | **26 passed** |
| `npm run test -- --run` | `client/` | **1 passed**（报告章节常量契约） |
| `npm run typecheck` | `client/` | 通过 |

CI： [`.github/workflows/ci.yml`](../.github/workflows/ci.yml)（backend pytest + client test + typecheck）。

## 2. 新增/强化的测试资产

| 资产 | 路径 |
|------|------|
| 模型列表 / GET compare / 对比 PDF / PATCH notes | [`server/tests/test_models_api.py`](../server/tests/test_models_api.py) |
| 422 校验抽样 | [`server/tests/test_api_robustness.py`](../server/tests/test_api_robustness.py) |
| 报告章节选项与后端令牌 | [`client/src/constants/reportSections.ts`](../client/src/constants/reportSections.ts) + `reportSections.test.ts` |
| 手动触发 acceptance | [`.github/workflows/acceptance.yml`](../.github/workflows/acceptance.yml) |

## 3. 文档与矩阵

| 文档 | 用途 |
|------|------|
| [`验收-AC证据矩阵.md`](验收-AC证据矩阵.md) | AC ↔ 自动化 / 手工 / 日期 |
| [`验收追踪.md`](验收追踪.md) | 实现# ↔ 自动化（已链到矩阵） |
| [`迭代计划.md`](迭代计划.md) 缺口表 | `GAP-G1-CONSENSUS-001` 口头共识规则 |

## 4. 签核（§四）

> **仅当** [`功能需求与验收状态.md`](功能需求与验收状态.md) §二 在 **当期发布口径** 下全部为 **通过** 后，再填写该文档 §四 六角色表。

| 角色 | 签核人 | 日期 | 结论 | 备注 |
|------|--------|------|------|------|
| 数据分析 | | | | |
| 模型训练 | | | | |
| AI 学生 | | | | |
| 产品设计 | | | | |
| 架构 | | | | |
| 测试顾问 | | | | |

---

*后续批次：按矩阵逐模块勾选 AC，更新「最后证据日期」与结论列。*
