# 验收抽样（F3 · 分模块）

> 对应路线图 **F3**：从 [`验收标准文档.md`](../product/验收标准文档.md) 各模块各抽 ≥1 条 AC，记录 **结果 / 验证方式 / 备注**（2026-04-05 批次补充）。

| AC 编号 | 模块 | 结果 | 验证方式 | 备注 |
|---------|------|------|----------|------|
| AC-0-01 | 智能向导 | **部分通过（API 等效）** | `pytest` `test_titanic_ac001_api_pipeline_report_pdf` | 与文档 Step 0–5 **后端等价**（Titanic 上传→划分→训练完成→PDF）；**SmartWorkflow UI 点选**仍须 A 档人工勾选 |
| AC-1-01 | 数据导入 | **部分通过（API）** | `test_upload_*`、`非法扩展` 用例 | CSV 上传与拒绝非表格已覆盖；Excel 多 Sheet 等见 AC-1-02 延后 |
| AC-9-01 | 分析报告（图表） | **未测 UI** | — | PDF 报告含 `evaluation`/`shap` 等章节由 Titanic 全链测试间接覆盖；独立「特征重要性图」交互未测 |
| AC-10-03 | 预测 | **部分通过（API 负例）** | `test_prediction_batch_summary_unknown_task` | 批量结果摘要 404；完整批量预测 UI 未在本抽样执行 |

**回归（本批次）**：`cd server && uv run pytest -q`；`cd client && npm run typecheck`。
